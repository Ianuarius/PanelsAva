<Window xmlns="https://github.com/avaloniaui"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:views="clr-namespace:PanelsAva.Views"
		mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="720" Width="1280" Height="720"
		x:Class="PanelsAva.Views.MainWindow"
		Icon="/Assets/avalonia-logo.ico"
		Title="PanelsAva">
	<DockPanel>
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="Window">
				<MenuItem x:Name="LayersMenuItem" Header="Layers" IsChecked="True" Click="OnToggleLayers" />
				<MenuItem x:Name="PropertiesMenuItem" Header="Properties" IsChecked="True" Click="OnToggleProperties" />
				<MenuItem x:Name="ColorMenuItem" Header="Color" IsChecked="True" Click="OnToggleColor" />
				<MenuItem x:Name="BrushesMenuItem" Header="Brushes" IsChecked="True" Click="OnToggleBrushes" />
				<MenuItem x:Name="HistoryMenuItem" Header="History" IsChecked="True" Click="OnToggleHistory" />
				<MenuItem x:Name="TimelineMenuItem" Header="Timeline" IsChecked="True" Click="OnToggleTimeline" />
			</MenuItem>
		</Menu>
		<Grid>
			<views:MainView x:Name="MainView" />
			<Canvas x:Name="FloatingLayer" Panel.ZIndex="10" />
		</Grid>
	</DockPanel>
</Window>